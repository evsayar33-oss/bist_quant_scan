name: BIST Daily Quant Scan

on:
  schedule:
    # Türkiye Saati ile 11:05 (UTC 08:05) - Sadece Hafta İçi (Pzt-Cuma: 1-5)
    - cron: '5 8 * * 1-5'
  workflow_dispatch: # İstediğin an elinle 'Run workflow' yapabilme butonu

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run Pipeline
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        python main.py

    - name: Commit & Push Changes
      run: |
        git config --global user.name "QuantBot"
        git config --global user.email "quantbot@github.com"
        git add gecmis_veri.csv
        git diff --quiet && git diff --staged --quiet || (git commit -m "QuantBot Veri Guncelleme: $(date)" && git push)
